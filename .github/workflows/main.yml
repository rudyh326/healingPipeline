name: ReactYarn CI

on:
  push:
    branches: [ "master" ]
    
jobs:
  build:

    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v3
    
    - run: npm install -g yarn
    - run: yarn install
    - run: |
        $env:HOST = 'localhost'
        yarn start





